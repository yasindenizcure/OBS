@model List<DenemeDers.Entity.Ogrenci>

@{
    ViewData["Title"] = "Öğrenci Listesi";
    Layout = null;
}

<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Öğrenci Paneli</title>

    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        :root {
            --bg-color: #f4f7f6;
            --card-bg: #ffffff;
            --primary-accent: #6366f1;
            --text-main: #1e293b;
            --text-muted: #64748b;
            --danger: #ef4444;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
            background-color: var(--bg-color);
            color: var(--text-main);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            width: 100%;
            max-width: 1100px;
            background: var(--card-bg);
            border-radius: 20px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.03), 0 1px 3px rgba(0, 0, 0, 0.01);
            padding: 40px;
        }

        .header-section {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 1px solid #f1f5f9;
        }

        .header-actions {
            display: flex;
            gap: 12px;
            align-items: center;
        }

        h1 {
            font-size: 1.5rem;
            font-weight: 700;
            letter-spacing: -0.5px;
        }
        .btn-base {
            text-decoration: none;
            padding: 10px 20px;
            border-radius: 10px;
            font-size: 0.85rem;
            font-weight: 600;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .btn-create {
            background-color: var(--primary-accent);
            color: white;
            box-shadow: 0 4px 12px rgba(99, 102, 241, 0.2);
        }

            .btn-create:hover {
                transform: translateY(-2px);
                box-shadow: 0 6px 15px rgba(99, 102, 241, 0.3);
                filter: brightness(1.1);
            }

        .btn-logout {
            background-color: transparent;
            color: var(--danger);
            border: 1.5px solid var(--danger);
        }

            .btn-logout:hover {
                background-color: var(--danger);
                color: white;
                box-shadow: 0 4px 12px rgba(239, 68, 68, 0.2);
            }
        table {
            width: 100%;
            border-collapse: collapse;
        }

        thead th {
            text-align: left;
            padding: 15px;
            color: var(--text-muted);
            font-size: 0.8rem;
            font-weight: 600;
            text-transform: uppercase;
            border-bottom: 1px solid #f1f5f9;
        }

        tbody tr {
            border-bottom: 1px solid #f8fafc;
            transition: background 0.2s;
        }

            tbody tr:hover {
                background-color: #fafbff;
            }

        tbody td {
            padding: 18px 15px;
            font-size: 0.9rem;
        }

        .student-name {
            font-weight: 600;
            color: #0f172a;
        }

        .dept-tag {
            background: #eef2ff;
            color: #4338ca;
            padding: 4px 10px;
            border-radius: 6px;
            font-size: 0.75rem;
            font-weight: 500;
        }

        .btn-action {
            text-decoration: none;
            padding: 6px 12px;
            border-radius: 6px;
            font-size: 0.8rem;
            font-weight: 600;
            margin-left: 8px;
            transition: all 0.2s;
        }

        .btn-back {
            background-color: transparent;
            color: var(--text-muted);
            border: 1.5px solid #e2e8f0;
            margin-right: auto; 
        }

            .btn-back:hover {
                background-color: #f8fafc;
                color: var(--primary-accent);
                border-color: var(--primary-accent);
            }

        .btn-edit {
            color: var(--primary-accent);
            background: rgba(99, 102, 241, 0.08);
        }

            .btn-edit:hover {
                background: var(--primary-accent);
                color: white;
            }

        .btn-delete {
            color: #ef4444;
            background: rgba(239, 68, 68, 0.08);
        }

            .btn-delete:hover {
                background: #ef4444;
                color: white;
            }
    </style>
</head>
<body>

    <div class="container">
        <div class="header-section">
            <div style="display: flex; align-items: center; gap: 20px;">
                <a href="/Dashboard/Index" class="btn-base btn-back">
                    <i class="fas fa-arrow-left"></i> Geri Dön
                </a>
                <h1>Öğrenci Listesi</h1>
            </div>

            <div class="header-actions">
                <a asp-action="Kayit" class="btn-base btn-create">
                    <i class="fas fa-plus"></i> Yeni Kayıt
                </a>
                <a href="/Login/Logout" class="btn-base btn-logout">
                    <i class="fas fa-power-off"></i> Güvenli Çıkış
                </a>
            </div>
        </div>

        <table>
            <thead>
                <tr>
                    <th>Öğrenci</th>
                    <th>Yaş</th>
                    <th>Sınıf</th>
                    <th>No</th>
                    <th>Bölüm</th>
                    <th style="text-align: right;">Yönetim</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var ogr in Model)
                {
                    <tr>
                        <td>
                            <div class="student-name">@ogr.Ad @ogr.Soyad</div>
                        </td>
                        <td>@ogr.Yas</td>
                        <td>@ogr.Sinif</td>
                        <td style="color: var(--text-muted)">#@ogr.OgrNo</td>
                        <td>
                            <span class="dept-tag">@ogr.Bolum.BolumAdi</span>
                        </td>
                        <td style="text-align: right;">
                            <a asp-action="Guncelle" asp-route-id="@ogr.OgrenciId" class="btn-action btn-edit">Düzenle</a>
                            <a asp-action="Sil" asp-route-id="@ogr.OgrenciId" class="btn-action btn-delete" onclick="return confirm('Bu öğrenciyi silmek istediğinize emin misiniz?')">Sil</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

</body>
</html>